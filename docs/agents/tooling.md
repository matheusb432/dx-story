# Tooling

- Tool versions and setup: [mise.toml](../../mise.toml) and [rust-toolchain.toml](../../rust-toolchain.toml).
- Available commands: `just --list`; definitions live in [justfile](../../justfile).
